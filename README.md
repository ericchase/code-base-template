A thought-out folder structure for organizing software development projects.

I start with the `Code` directory straight on my `C:/` drive, which typically corresponds to the `/` root directory on Linux. Not sure about macOS.
